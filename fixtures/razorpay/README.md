# Razorpay contract fixtures

These payloads are sanitized, deterministic, **SYNTHETIC** Razorpay-shaped test
fixtures. They contain no production merchant or customer data and must not be
represented as real Razorpay events or merchant results.

`manifest.json` records their synthetic provenance and the immutable context
used to produce each normalized snapshot under `snapshots/`.
